#!/usr/bin/python3
def add_tuple(tuple_a=(), tuple_b=()):
    my_new_tuple = ()
    tuple_a_m = tuple_a + (0, 0)
    tuple_b_m = tuple_b + (0, 0)
    my_new_tuple = tuple_a_m[0] + tuple_b_m[0], tuple_a_m[1] + tuple_2[1]
    return my_new_tuple
